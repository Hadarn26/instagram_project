const pageContainerStyles = {
  maxWidth: 420,
  mx: "auto",
  height: "100vh",
  display: "flex",
  flexDirection: "column",
  backgroundColor: "#fff",
};

const postsContainerStyles = {
  flex: 1,
  overflowY: "auto",
};

const messageStyles = {
  p: 2,
};

export { pageContainerStyles, postsContainerStyles, messageStyles };
