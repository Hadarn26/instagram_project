const cardStyles = {
    mb: 3,
    borderRadius: 0,
    boxShadow: "none",
    borderBottom: "1px solid #ddd",
};

const userRowStyles = {
    display: "flex",
    alignItems: "center",
    gap: 1,
    px: 1.5,
    py: 1,
};

const avatarStyles = {
    width: 32,
    height: 32,
};

const imageStyles = {
    width: "100%",
    aspectRatio: "4 / 3",
    objectFit: "cover",
};

const contentStyles = {
    py: 1,
};

const likedIconStyles = {
    color: "red",
};

export { cardStyles, userRowStyles, avatarStyles, imageStyles, contentStyles, likedIconStyles };